Ext.define('Vega.view.development.sample.edit.DetailModel', {
    extend: 'Ext.app.ViewModel',

    requires: [
        'Vega.model.Bom'
    ],

    alias: 'viewmodel.sample-edit-detail',

    formulas: {
        extcost: {
            get: function(get){
                return get('qty') * get('cost');
            },

            set: function(value){

            }
        }
    },

    stores: {

    }

});
